---
phase: 7
plan: 2
wave: 2
depends_on: ["07-01"]
files_modified: ["matriculaup_app/windows/runner/Runner.rc", "scripts/build_flutter_exe.ps1"]
autonomous: false
must_haves:
  truths:
    - "A build script exists to easily compile the Flutter app for Windows."
    - "The Windows app name is set properly (e.g. MatriculaUp)."
  artifacts:
    - "scripts/build_flutter_exe.ps1"
---

# Plan 7.2: Desktop Distribution

<objective>
Prepare the Flutter application for Windows distribution. We need to define a clear build script that packages the release version for end-users, similar to the PyInstaller setup for the Python version.
</objective>

<context>
Load for context:
- `matriculaup_app/windows/runner/Runner.rc` (To check app branding/titles)
- `.gsd/ROADMAP.md` (To verify release requirements)
</context>

<tasks>

<task type="auto">
  <name>Update Windows Runner Branding</name>
  <files>matriculaup_app/windows/runner/Runner.rc, matriculaup_app/windows/runner/main.cpp</files>
  <action>
    Update the Window Title in `main.cpp` (look for `window.Create`) to "MatriculaUp".
    Update `Runner.rc` `VERSIONINFO` block to set `FileDescription` and `ProductName` to "MatriculaUp" instead of "matriculaup_app".
  </action>
  <verify>Check cpp and rc files for string replacements.</verify>
  <done>Branding strings match the product name.</done>
</task>

<task type="auto">
  <name>Create Build Script</name>
  <files>scripts/build_flutter_exe.ps1</files>
  <action>
    Create a fresh PowerShell script `build_flutter_exe.ps1`.
    It should enter the `matriculaup_app` directory, run `flutter clean`, then `flutter build windows --release`.
    Finally it should echo the path to the resulting `Release` directory so the user knows where the .exe is.
  </action>
  <verify>Check the shell script structure and commands.</verify>
  <done>User has a single script to generate the Windows release builder.</done>
</task>

</tasks>

<verification>
After tasks, verify:
- [ ] `flutter build windows` works (pending Developer Mode on user PC).
- [ ] Window title bar says "MatriculaUp".
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
