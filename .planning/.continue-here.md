---
context: default
phase: 2026-II V1 data refresh
task: null
total_tasks: null
status: paused
last_updated: 2026-07-08T04:46:41.255Z
---

<current_state>
MatriculaUp has been refreshed and published for 2026-II V1.

The work was an ad-hoc data refresh, not a new active phase under `.planning/phases`, so this handoff lives at `.planning/.continue-here.md`.

Important external state:
- Correct GitHub repo: `https://github.com/johnbarraza/MatriculaUp.git`
- Local workspace `origin` is wrong: `https://github.com/toonight/get-shit-done-for-antigravity.git`
- Main commit published: `e3bf1b8` (`Update EFEs SSU for 2026-II V1`)
- GitHub Pages commit published: `9b44f18` (`deploy: e3bf1b834ee304e60cd282f749a8c0d4776de1fc`)
</current_state>

<completed_work>

Completed:
- Extracted regular offer from `pdfs/matricula/2026-2/regular/Oferta-Academica-2026-II-V1.pdf`.
- Generated `input/courses_2026-2_v1.json`.
- Updated `matriculaup_app/assets/default_courses.json`.
- Added active calendar `matriculaup_app/assets/calendar_2026-2.json`.
- Reworked `scripts/extractors/efe_ssu.py` for the 2026-II EFE PDF format.
- Reworked `scripts/extractors/efe_to_courses.py` to convert EFE/SSU raw JSON to the app course schema.
- Extracted EFE/SSU 2026-II V1 from:
  - `pdfs/matricula/2026-2/EFEs/Horarios-ofertados-matricula-2026-II-planes-antiguos.pdf`
  - `pdfs/matricula/2026-2/EFEs/Sesiones SSU 2026-II.xlsx`
- Added source EFE files, including both `planes-antiguos` and `planes-actuales` PDFs.
- Generated `pdfs/matricula/2026-2/EFEs/efe_ssu_2026-2_v1.json`.
- Generated `input/efe_courses_2026-2_v1.json`.
- Added bundled asset `matriculaup_app/assets/efe_courses_2026-2_v1.json`.
- Re-enabled `DataLoader.loadDefaultEfeCourses()` to load `assets/efe_courses_2026-2_v1.json`.
- Updated release scripts and installer to include `efe_courses_2026-2_v1.json`.
- Changed feedback/contact URL in `matriculaup_app/lib/ui/pages/home_page.dart` to `https://forms.gle/cag87CjtGhmDLaek6`.
- Built and launched local Windows debug app.
- Deployed GitHub Pages.
</completed_work>

<verification>

Commands/results already run:
- `python -m pytest -q tests\test_extraction.py tests\test_validation.py` -> `22 passed`
- JSON validator:
  - `input/courses_2026-2_v1.json` -> 394 courses, 0 errors
  - `input/efe_courses_2026-2_v1.json` -> 48 courses, 0 errors
  - `matriculaup_app/assets/default_courses.json` -> 394 courses, 0 errors
  - `matriculaup_app/assets/efe_courses_2026-2_v1.json` -> 48 courses, 0 errors
- `flutter analyze` -> no issues
- `flutter test` -> all tests passed
- `flutter build windows --debug` -> OK
- `flutter build web --release --base-href /MatriculaUp/` -> OK

Public Pages verification:
- `https://johnbarraza.github.io/MatriculaUp/assets/assets/default_courses.json` served `ciclo=2026-2`, `version=v1`, `cursos=394`.
- `https://johnbarraza.github.io/MatriculaUp/assets/assets/efe_courses_2026-2_v1.json` served `ciclo=2026-2`, `version=v1`, `cursos=48`.
- `https://johnbarraza.github.io/MatriculaUp/main.dart.js` contained `https://forms.gle/cag87CjtGhmDLaek6` and did not contain the old Forms URL.
</verification>

<remaining_work>

No required work remains for the 2026-II V1 refresh.

Optional follow-ups:
- Add analytics only after the user chooses a provider and supplies a tracking ID/script. Options discussed: GA4, Plausible, Umami.
- Add an in-app switch or separate asset for EFE `planes-actuales` if users on 2022-2024 plans need that view. Current bundled default is `planes-antiguos`.
- Consider committing future data refreshes from a clean clone or fixing the local `origin` to avoid accidental pushes to the wrong repo.
</remaining_work>

<decisions_made>

- Bundled EFE default uses `planes-antiguos`, because the prior app flow targeted Economia 2017/older plans.
- The EFE extractor is parameterized so `planes-actuales` can be generated later without another rewrite.
- SSU sessions are enriched from Excel and converted into normal app sessions.
- Exact-date EFE sessions are preserved as sessions with a `fecha` field and readable observations.
- Contact/feedback now uses `https://forms.gle/cag87CjtGhmDLaek6`.
- Publishing is done through temporary clones of `johnbarraza/MatriculaUp` because the local workspace remote is unsafe.
</decisions_made>

<blockers>

None.
</blockers>

## Required Reading

1. `.planning/HANDOFF.json` - machine-readable summary of this paused state.
2. `.planning/PROJECT.md` - current project context and active data sources.
3. `scripts/extractors/efe_ssu.py` - EFE/SSU raw extractor.
4. `scripts/extractors/efe_to_courses.py` - conversion from raw EFE JSON to app course JSON.
5. `matriculaup_app/lib/data/data_loader.dart` - bundled regular/EFE/calendar asset loading.
6. `matriculaup_app/lib/ui/pages/home_page.dart` - contact link and top-level UI.

## Critical Anti-Patterns

- Do not push from the local workspace without checking remotes first. The local `origin` points to the wrong repository.
- Do not mix stale 2026-I EFE data with 2026-II regular data. EFE is now loaded from `efe_courses_2026-2_v1.json`.
- Do not add analytics snippets without a concrete tracking ID/provider decision.

## Infrastructure State

- Local debug app was relaunched from `matriculaup_app/build/windows/x64/runner/Debug/matriculaup_app.exe`.
- GitHub Pages branch `gh-pages` is deployed at commit `9b44f18`.
- Main branch is at commit `e3bf1b8` for the EFE/SSU refresh before this handoff commit.

<context>
The user asked to continue from previous work, include EFE/SSU, verify locally, update contact link, commit/push, and leave a handoff for another session. All required code/data/deploy work is complete. This handoff is the final continuity artifact.
</context>

<next_action>
Start with: verify the latest `main` commit on `johnbarraza/MatriculaUp`, then read `.planning/HANDOFF.json` and this file before making any new data or deploy changes.
</next_action>
